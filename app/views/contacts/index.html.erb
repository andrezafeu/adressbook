<ul>
	<% @contacts.each do | contact | %>
		<li>
			<%= contact.name %>
			<%= contact.address %>
			<%= contact.phone %>
			<%= contact.email %>
		</li>
	<% end %>
</ul>

<%= link_to 'Add a new contact', new_path %>