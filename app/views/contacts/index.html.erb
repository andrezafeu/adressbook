<ul>
	<% @contacts.each do | contact | %>
		<li>
			<%= link_to contact.name, show_path(contact) %>
			<%= contact.address %>
			<%= contact.phone %>
			<%= contact.email %>
		</li>
	<% end %>
</ul>

<%= link_to 'Add a new contact', new_path %>